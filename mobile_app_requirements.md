# 家具销售提成管理系统 - 员工App页面详细功能设计文档

---

## 文档版本控制

| 版本 | 日期 | 作者 | 变更说明 |
|------|------|------|----------|
| v1.0 | 2026-05-07 | 产品组 | 初始版本，基于实际项目结构 |

---

## 1. 页面结构与导航设计

### 1.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    员工App架构图                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐ │
│  │   首页      │    │   订单管理  │    │   业绩中心  │ │
│  │ (Dashboard) │    │  (Orders)   │    │ (Performance)││
│  └──────┬──────┘    └──────┬──────┘    └──────┬──────┘ │
│         │                  │                  │         │
│         ▼                  ▼                  ▼         │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐ │
│  │   库存查询  │    │    我的     │    │  订单详情   │ │
│  │ (Inventory)│    │  (Profile)  │    │ (OrderDetail)││
│  └─────────────┘    └─────────────┘    └─────────────┘ │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 1.2 底部导航栏（Tab Bar）

| Tab | 图标 | 名称 | 路径 | 可见角色 |
|-----|------|------|------|----------|
| 首页 | 🏠 | 首页 | `/home` | 所有角色 |
| 订单 | 📋 | 订单 | `/orders` | 业务员、主管、店长 |
| 业绩 | 📊 | 业绩 | `/performance` | 所有角色 |
| 库存 | 📦 | 库存 | `/inventory` | 仓管、店长、主管 |
| 我的 | 👤 | 我的 | `/profile` | 所有角色 |

### 1.3 页面路由结构

```
App
├── HomePage (首页)
│   └── OrderQuickCreate (快速下单)
├── OrdersPage (订单列表)
│   ├── OrderDetail (订单详情)
│   ├── OrderCreate (新建订单)
│   └── PaymentRecord (回款记录)
├── PerformancePage (业绩中心)
│   └── CommissionDetail (提成详情)
├── InventoryPage (库存查询)
│   └── StockDetail (库存详情)
└── ProfilePage (个人中心)
    ├── EditProfile (编辑资料)
    ├── ChangePassword (修改密码)
    └── Settings (设置)
```

---

## 2. 功能模块详细说明

### 2.1 首页模块（Dashboard）

#### 2.1.1 页面布局

```
┌─────────────────────────────────────┐
│  顶部区域                           │
│  ┌───────────┬───────────────────┐ │
│  │ 头像/姓名 │ 门店名称           │ │
│  └───────────┴───────────────────┘ │
├─────────────────────────────────────┤
│  数据卡片区域                       │
│  ┌───────┐ ┌───────┐ ┌───────┐    │
│  │本月业绩│ │本月提成│ │待办事项│    │
│  │ ¥XXX  │ │ ¥XXX  │ │  X件  │    │
│  └───────┘ └───────┘ └───────┘    │
├─────────────────────────────────────┤
│  快捷操作区域                       │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐   │
│  │下单 │ │回款 │ │客户 │ │报表 │   │
│  └─────┘ └─────┘ └─────┘ └─────┘   │
├─────────────────────────────────────┤
│  订单动态列表                       │
│  ┌───────────────────────────────┐  │
│  │ 订单号：DD20260504001         │  │
│  │ 客户：张三 金额：¥5,000        │  │
│  │ 状态：待审核                   │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

#### 2.1.2 功能点详解

| 功能点 | 功能描述 | 权限要求 | API接口 |
|--------|----------|----------|----------|
| 业绩概览 | 展示本月销售额、提成、待办数量 | 所有角色 | `/api/dashboard/overview` |
| 快捷下单 | 快速创建新订单入口 | 业务员、主管、店长 | `/api/orders/create` |
| 快捷回款 | 快速录入回款入口 | 业务员、主管、店长 | `/api/payments/create` |
| 客户管理 | 客户列表入口 | 业务员、主管、店长 | `/api/customers` |
| 订单动态 | 展示最近订单状态变化 | 所有角色 | `/api/orders/feed` |

### 2.2 订单管理模块

#### 2.2.1 订单列表页面

| 功能点 | 功能描述 | 权限要求 | API接口 |
|--------|----------|----------|----------|
| 订单列表 | 分页展示订单，支持状态筛选 | 所有角色 | `/api/orders` |
| 订单搜索 | 按订单号、客户姓名搜索 | 所有角色 | `/api/orders/search` |
| 状态筛选 | 待审核、已生效、已驳回、已取消 | 所有角色 | `/api/orders?status=X` |
| 时间筛选 | 今日、本周、本月、自定义 | 所有角色 | `/api/orders?startDate=&endDate=` |

#### 2.2.2 订单创建页面

| 功能点 | 功能描述 | 权限要求 | API接口 |
|--------|----------|----------|----------|
| 客户信息录入 | 选择/搜索客户，填写联系方式 | 业务员、主管、店长 | `/api/customers/search` |
| 商品选择 | 搜索并添加商品到订单 | 业务员、主管、店长 | `/api/products/search` |
| 订单类型选择 | 单品/多品/特殊审批/同行带单 | 业务员、主管、店长 | - |
| 价格调整 | 输入成交价格，自动计算折扣 | 业务员、主管、店长 | - |
| 礼品选择 | 选择赠送礼品 | 业务员、主管、店长 | `/api/gifts` |
| 订单提交 | 提交订单进入审核流程 | 业务员、主管、店长 | `/api/orders/create` |

#### 2.2.3 订单详情页面

| 功能点 | 功能描述 | 权限要求 | API接口 |
|--------|----------|----------|----------|
| 订单信息展示 | 订单基本信息、商品明细、礼品信息 | 所有角色 | `/api/orders/{id}` |
| 订单审核 | 审批通过/驳回订单 | 主管、店长 | `/api/orders/{id}/approve` |
| 回款录入 | 录入回款金额 | 业务员、主管、店长 | `/api/payments/create` |
| 回款记录 | 查看订单所有回款记录 | 所有角色 | `/api/payments?orderId={id}` |
| 退货处理 | 发起退货申请 | 主管、店长 | `/api/orders/{id}/return` |

### 2.3 业绩中心模块

#### 2.3.1 业绩概览页面

| 功能点 | 功能描述 | 权限要求 | API接口 |
|--------|----------|----------|----------|
| 业绩统计 | 展示业绩完成率、排名、趋势 | 所有角色 | `/api/performance/overview` |
| 提成统计 | 本月提成、累计提成、提成明细 | 所有角色 | `/api/commissions` |
| 订单统计 | 订单数量、客单价、转化率 | 所有角色 | `/api/orders/stats` |
| 团队排行 | 团队成员业绩排名 | 主管、店长 | `/api/performance/team` |

#### 2.3.2 提成详情页面

| 功能点 | 功能描述 | 权限要求 | API接口 |
|--------|----------|----------|----------|
| 提成列表 | 按订单展示提成明细 | 所有角色 | `/api/commissions` |
| 提成计算说明 | 展示提成计算规则和公式 | 所有角色 | - |
| 老带新奖励 | 展示引荐新员工获得的奖励 | 所有角色 | `/api/commissions/referral` |

### 2.4 库存查询模块

#### 2.4.1 库存列表页面

| 功能点 | 功能描述 | 权限要求 | API接口 |
|--------|----------|----------|----------|
| 库存查询 | 按商品名称、SKU搜索库存 | 仓管、主管、店长 | `/api/warehouse/stocks` |
| 库存预警 | 展示库存低于预警阈值的商品 | 仓管、主管、店长 | `/api/stock/alerts` |
| 仓库切换 | 多仓库场景下切换仓库查看 | 仓管、主管、店长 | `/api/warehouses` |

#### 2.4.2 库存详情页面

| 功能点 | 功能描述 | 权限要求 | API接口 |
|--------|----------|----------|----------|
| 库存详情 | 展示库存数量、批次信息 | 仓管、主管、店长 | `/api/warehouse/stocks/{id}` |
| 库存流水 | 查看库存变动记录 | 仓管、主管、店长 | `/api/inventory/transactions` |

### 2.5 个人中心模块

#### 2.5.1 个人信息页面

| 功能点 | 功能描述 | 权限要求 | API接口 |
|--------|----------|----------|----------|
| 个人信息展示 | 姓名、职位、入职日期、门店 | 所有角色 | `/api/users/me` |
| 编辑资料 | 修改个人信息 | 所有角色 | `/api/users/{id}` |
| 修改密码 | 更改登录密码 | 所有角色 | `/api/users/{id}/password` |

#### 2.5.2 设置页面

| 功能点 | 功能描述 | 权限要求 | API接口 |
|--------|----------|----------|----------|
| 系统设置 | 通知设置、清除缓存 | 所有角色 | - |
| 关于我们 | 版本信息、帮助文档 | 所有角色 | - |
| 退出登录 | 安全退出账号 | 所有角色 | `/api/logout` |

---

## 3. 交互逻辑设计

### 3.1 通用交互规则

| 交互类型 | 设计规范 | 示例 |
|----------|----------|------|
| 列表刷新 | 下拉刷新获取最新数据 | 订单列表下拉刷新 |
| 加载更多 | 上拉加载下一页数据 | 订单列表滚动加载 |
| 表单提交 | 提交前验证必填项，显示loading状态 | 新建订单提交 |
| 确认弹窗 | 重要操作前二次确认 | 删除订单、提交审核 |
| Toast提示 | 操作成功/失败提示，3秒自动消失 | 订单提交成功 |
| 网络异常 | 显示网络错误提示，提供重试按钮 | 网络超时提示 |

### 3.2 订单创建流程

```
选择客户 ──► 选择商品 ──► 设置价格 ──► 选择礼品 ──► 确认提交
     │              │            │            │
     ▼              ▼            ▼            ▼
 搜索客户      搜索商品      自动计算      礼品列表
 新建客户      规格选择      利润提成      多选支持
```

### 3.3 订单审核流程

```
业务员提交 ──► 主管/店长审核 ──► 订单生效
                    │
                    ├─► 通过 ──► 库存扣减
                    └─► 驳回 ──► 退回修改
```

### 3.4 回款录入流程

```
选择订单 ──► 输入回款金额 ──► 选择回款方式 ──► 提交审核
                                          │
                                          ▼
                                   老板审核通过
                                   计入提成计算
```

---

## 4. 界面元素规范

### 4.1 设计原则

| 原则 | 说明 |
|------|------|
| 简洁高效 | 减少操作步骤，一键直达核心功能 |
| 信息清晰 | 数据展示层次分明，重点突出 |
| 操作友好 | 按钮尺寸适合移动端点击（最小44px） |
| 视觉统一 | 颜色、字体、间距保持一致性 |

### 4.2 颜色规范

| 颜色类型 | 颜色值 | 使用场景 |
|----------|--------|----------|
| 主色调 | #1890ff | 按钮、链接、选中状态 |
| 成功色 | #52c41a | 审核通过、操作成功 |
| 警告色 | #faad14 | 待审核、库存预警 |
| 危险色 | #f5222d | 驳回、删除、错误提示 |
| 背景色 | #f5f5f5 | 页面背景 |
| 卡片背景 | #ffffff | 卡片、列表项背景 |

### 4.3 字体规范

| 字体类型 | 字号 | 使用场景 |
|----------|------|----------|
| 标题 | 18sp | 页面标题 |
| 正文 | 16sp | 内容文字 |
| 辅助文字 | 14sp | 描述文字 |
| 小字 | 12sp | 提示文字、时间 |

### 4.4 间距规范

| 间距类型 | 数值 | 使用场景 |
|----------|------|----------|
| 页面边距 | 16px | 页面内容与屏幕边缘 |
| 卡片间距 | 12px | 卡片之间间距 |
| 内容间距 | 8px | 元素内部间距 |

---

## 5. 数据流转规则

### 5.1 订单数据流转

```
┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐
│  创建订单  │ ──► │  审核订单  │ ──► │  订单生效  │ ──► │  回款完成  │
└─────────┘     └─────────┘     └─────────┘     └─────────┘
      │               │               │               │
      ▼               ▼               ▼               ▼
   orders         orders         orders         payments
   order_items    order_items    warehouse_     commissions
                  order_gifts    stocks
```

### 5.2 提成数据流转

```
订单完成 ──► 回款确认 ──► 提成核算 ──► 工资结算
     │              │            │            │
     ▼              ▼            ▼            ▼
  orders        payments    commissions    salary_records
```

### 5.3 库存数据流转

```
采购入库 ──► 库存增加 ──► 销售出库 ──► 库存减少
     │              │            │            │
     ▼              ▼            ▼            ▼
purchase_    warehouse_    orders        warehouse_
orders       stocks        order_items   stocks
inventory_                           inventory_
batches                              transactions
```

---

## 6. 权限控制策略

### 6.1 角色权限矩阵

| 页面/功能 | 业务员 | 主管 | 店长 | 仓管 |
|-----------|--------|------|------|------|
| 首页 | ✓ | ✓ | ✓ | ✓ |
| 订单列表 | ✓(仅自己) | ✓ | ✓ | - |
| 新建订单 | ✓ | ✓ | ✓ | - |
| 订单审核 | - | ✓ | ✓ | - |
| 回款录入 | ✓ | ✓ | ✓ | - |
| 业绩中心 | ✓ | ✓ | ✓ | - |
| 库存查询 | - | ✓ | ✓ | ✓ |
| 个人中心 | ✓ | ✓ | ✓ | ✓ |

### 6.2 数据权限规则

| 角色 | 数据范围 |
|------|----------|
| 业务员 | 仅查看自己创建的订单和个人业绩 |
| 主管 | 查看自己及下属业务员的订单和业绩 |
| 店长 | 查看全门店所有订单和业绩 |
| 仓管 | 查看库存数据，无订单操作权限 |

### 6.3 权限校验流程

```
用户请求 ──► 登录状态校验 ──► 角色权限校验 ──► 数据权限校验 ──► 返回数据
                              │                    │
                              └──► 无权限 ──► 提示无权限
```

---

## 7. 异常处理机制

### 7.1 错误类型分类

| 错误类型 | 说明 | 处理方式 |
|----------|------|----------|
| 网络错误 | 网络连接失败、超时 | 显示错误提示，提供重试按钮 |
| 服务端错误 | API返回5xx错误 | 显示服务器繁忙，请稍后重试 |
| 权限错误 | 无权限访问资源 | 显示无权限提示 |
| 参数错误 | 请求参数无效 | 显示具体错误字段提示 |
| 业务错误 | 业务规则校验失败 | 显示业务错误描述 |

### 7.2 异常处理流程

```
请求API ──► 成功 ──► 处理数据 ──► 展示页面
     │
     └─► 失败
          │
          ├─► 网络错误 ──► 重试提示
          ├─► 权限错误 ──► 跳转登录
          └─► 业务错误 ──► 提示具体信息
```

### 7.3 重试机制

| 场景 | 重试次数 | 间隔时间 |
|------|----------|----------|
| 网络超时 | 3次 | 1s、2s、4s |
| 请求失败 | 1次 | 2s |

---

## 8. API对接说明

### 8.1 接口基础配置

| 配置项 | 值 |
|--------|------|
| 基础URL | `https://api.example.com/v1` |
| 请求超时 | 30秒 |
| 数据格式 | JSON |
| 字符编码 | UTF-8 |

### 8.2 认证方式

```
请求头：Authorization: Bearer {token}

登录流程：
1. POST /api/login 获取token
2. 将token存储到本地
3. 每次请求携带token
4. token过期自动跳转登录页
```

### 8.3 核心接口列表

| 模块 | 接口路径 | 方法 | 功能描述 |
|------|----------|------|----------|
| 认证 | `/api/login` | POST | 用户登录 |
| 认证 | `/api/logout` | POST | 用户退出 |
| 首页 | `/api/dashboard/overview` | GET | 首页数据概览 |
| 订单 | `/api/orders` | GET | 获取订单列表 |
| 订单 | `/api/orders` | POST | 创建订单 |
| 订单 | `/api/orders/{id}` | GET | 获取订单详情 |
| 订单 | `/api/orders/{id}/approve` | POST | 审核订单 |
| 回款 | `/api/payments` | POST | 录入回款 |
| 业绩 | `/api/performance/overview` | GET | 业绩概览 |
| 提成 | `/api/commissions` | GET | 提成明细 |
| 库存 | `/api/warehouse/stocks` | GET | 库存列表 |
| 商品 | `/api/products/search` | GET | 商品搜索 |
| 用户 | `/api/users/me` | GET | 获取当前用户信息 |

### 8.4 请求响应格式

**请求格式：**
```json
{
  "param1": "value1",
  "param2": "value2"
}
```

**成功响应：**
```json
{
  "code": 200,
  "message": "success",
  "data": { ... },
  "timestamp": 1777910097
}
```

**失败响应：**
```json
{
  "code": 400,
  "message": "参数错误",
  "errors": {
    "field": "错误描述"
  },
  "timestamp": 1777910097
}
```

### 8.5 接口错误码

| 错误码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未登录或token过期 |
| 403 | 无权限访问 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

---

## 附录：页面流程图

### A.1 订单创建流程

```
开始 ──► 选择客户 ──► 选择商品 ──► 填写价格 ──► 选择礼品
                                          │
                                          ▼
                                     确认订单 ──► 提交成功
                                          │
                                          └─► 提交失败 ──► 重新填写
```

### A.2 订单审核流程

```
待审核订单列表 ──► 查看订单详情 ──► 审核决策
                                          │
                            ┌─────────────┴─────────────┐
                            ▼                           ▼
                         通过审核                    驳回订单
                            │                           │
                            ▼                           ▼
                     订单生效                    订单状态更新
                     库存扣减                    通知业务员
```

---

**文档审批**

| 角色 | 姓名 | 审批日期 |
|------|------|----------|
| 产品负责人 | | |
| 技术负责人 | | |
| 业务负责人 | | |